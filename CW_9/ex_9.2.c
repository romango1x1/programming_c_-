/*#include <stdio.h>
#include <stdlib.h>
#include <math.h>

struct Stack {
    int data;
    struct Stack* addr;
}stack;

int main() {
    struct Stack s;
    int x, i = 0, a[ ];
    do {
        struct Stack t;
        scanf("%d", &x);
        // a[i++] = x;
        // int *mem;
        // mem = malloc(sizeof(x));
        // *mem = x;
        t.data = x;
        t.addr = malloc(sizeof(stack));
        t.addr = stack.addr;
        s.addr = NULL;
        s.data = 0;
    } while(x != 0);
    struct Stack t1;
    while(s.addr != NULL) {
        x = s.data;
        s = *(s.addr);
    }
}*/